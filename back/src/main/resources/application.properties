spring.application.name=back

# ── Base de datos Oracle Cloud ───────────────────────────────
# El wallet está en src/main/resources/wallet y se copia al classpath al compilar
# TNS_ADMIN apunta a la ruta absoluta del wallet en el proyecto
spring.datasource.url=jdbc:oracle:thin:@chiao123_low?TNS_ADMIN=src/main/resources/wallet
spring.datasource.username=RESTAURANTE_USER
spring.datasource.password=Pollitos01270
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# ── JPA / Hibernate ──────────────────────────────────────────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# ── Jackson — mantener camelCase en JSON ─────────────────────
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.default-property-inclusion=non_null

# ── JWT ──────────────────────────────────────────────────────
app.jwt.secret=pon_aqui_una_clave_de_32_o_mas_caracteres_123456789
app.jwt.expiration-ms=86400000

# ── Servidor ─────────────────────────────────────────────────
server.port=8080
